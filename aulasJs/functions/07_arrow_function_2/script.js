const multiplicarPorDois = (x) => {
    return x * 2;
}

console.log(multiplicarPorDois(2));

const multiplicarPorDois2 = x => x * 2; // Quando a arrow function só tem um parâmetro não precisa de chaves e return.

console.log(multiplicarPorDois2(4));